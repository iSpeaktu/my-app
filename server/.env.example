# Example environment variables for server
PORT=4000
# Supabase (or other DB) credentials - keep these secret
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# JWT secret if you intend to sign your own tokens
JWT_SECRET=
